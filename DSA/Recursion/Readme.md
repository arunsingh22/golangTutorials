# Head Recursion: 
- We pospond the tasks in favour of making function calls. And while returning back we take up these tasks and execute them.
- It's a lazy execution
- Post-order execution.


# Tail Recursion:
- It's a eger execution model.
- Pre-order execution.


# Foundational Coding Question on Recursion:
- Printing no 1 to n
- Printing no from n to 1
- Sum of digits using recursion
- Finding a element x in an array using recursion.
- Finding minimum/max in a array using recursion.
- Check if the array isSorted or not.
# Functional recursion:
- Factorial of a number.
- Fibonacci number.
- Tribonnaci Number
- reverse the array
- GCD(a,b) using recursion. aka Euclid's Algorithm.
    **RELATIONSHIP : GCD(a,b) × LCM(a,b) = a × b**
    - GCD(a,b) == GCD(a-b,b)
    But the subtaction can take multiple steps so  simple by doing the remainder we jump
    multiple steps to reach a smaller value ie GCD(a%b,b)

- Finding first and last Occurance of 'a' char in "abcdefabcdef"
- Move all X's to end of the String.
- Recursive Binary search.
-Check if the given string is a palindrome using recursion.
-Sorting a stack using recursion.

Reverse the number using recursion. https://www.geeksforgeeks.org/write-a-program-to-reverse-digits-of-a-number/?ref=leftbar-rightbar

Practice Questions on Recursion
https://www.geeksforgeeks.org/practice-questions-for-recursion-set-2/?ref=lbp
https://www.geeksforgeeks.org/practice-questions-for-recursion-set-3/?ref=lbp